# follow-me-climate 仓库约定

## Release 说明统一使用简体中文

本仓库面向中文用户，README 为中文，因此所有 GitHub Release 的正文一律用**简体中文**撰写，不再混用英文：

- **语言**：正文简体中文；技术名词保留英文原文（API/常量/错误信息/命令/文件路径，如 `ATTR_TEMPERATURE`、`EntityPlatformModule`、`target_temp_step`），不要强行翻译。
- **标题**：只用版本号（`vX.Y.Z`），不附加中英文描述。
- **正文结构**：沿用现行格式——修复类版本用 `## 修复`，功能类版本用 `## 新增`（需要时可加 `## 破坏性变更` 等小节）；版本内关键条目用「**加粗短语**：说明」的形式。涉及升级动作时以「**升级方式**：HACS → Follow-Me Climate → 更新，然后重启 Home Assistant」收尾。
- **发版流程**：`manifest.json` 的 `version` 与 tag 保持一致（`0.2.3` ↔ `v0.2.3`）；先推 main，再推 tag，然后发布正式 Release（非草稿）。

> 历史背景：v0.1.1–v0.2.1 的说明是英文，v0.2.2 起是中文；2026-08-26 已将旧版本统一翻译为中文，此后一律中文。
